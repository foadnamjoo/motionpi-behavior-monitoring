# Copy to .env and set your MongoDB connection string (from MongoDB Compass or your provider).
# Do not commit .env or any real credentials.

MONGODB_URI=mongodb://localhost:27017

# If your DB is behind SSH, run your tunnel script first, then use:
# MONGODB_URI=mongodb://localhost:27017
