# MotionPI Report — template for MongoDB connection. Copy to .env or config.env and set your URI.
# Do not commit .env or config.env with real credentials.
# If your MongoDB uses auth: MONGODB_URI=mongodb://USER:PASSWORD@host:27017

MONGODB_URI=mongodb://localhost:27017
