# MotionPI Report — database connection.
# Keep this file in the same folder as "MotionPI Report".
# Replace placeholder before distribution. Do not commit real credentials.

MONGODB_URI=mongodb://admin:YOUR_MONGODB_PASSWORD@127.0.0.1:27017/?authSource=admin&directConnection=true
